<h1>New comments</h1>

<%= form_with model: @comment, url: new_comment_path  do |f| %>
  <div>
    <%= f.label :Text %><br>
    <%= f.text_area :text, size: "70x5"%>
  </div>
  <div>
    <%= f.submit "Add Comment" %>
  </div>
<% end %>